<div class="col-6 col-lg-4 mb-4">
  <a href="{{ item.url }}">
    {% assign imgsplit = item.data.image | split: '/' %}
    <img
      src="{{ item.data.image}}"
      alt="{{ item.data.title }}"
      class="mb-4 full"></a>
  <h4 class="text-2xl">
    <a href="{{ item.url }}">{{ item.data.title }}</a>
  </h4>
</div>